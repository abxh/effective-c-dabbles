

int main(void) {
    int x = 5; // simple statement

    // compound statement:
    {
        // push y in stack
        int y;
        y = 6;
        y++;
    }
    // y is released from stack (or it seems)
}
